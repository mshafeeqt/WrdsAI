<!-- <!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <style>
      body {
        font-family: Arial, sans-serif;
        padding: 40px;
        line-height: 1.6;
      }

      .title {
        text-align: center;
        font-size: 26px;
        font-weight: bold;
        margin-bottom: 25px;
      }

      .section-title {
        font-size: 18px;
        font-weight: bold;
        margin-top: 25px;
        margin-bottom: 10px;
        border-bottom: 1px solid #555;
        padding-bottom: 5px;
      }

      .row {
        margin-bottom: 8px;
      }

      .label {
        font-weight: bold;
      }

      table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 10px;
      }

      td {
        padding: 8px 0;
      }

      .footer {
        margin-top: 40px;
        text-align: center;
        font-size: 12px;
        color: #666;
      }
    </style>
  </head>

  <body>
    <div class="title">PAYMENT RECEIPT</div>

    <!-- <div class="row"><span class="label">Receipt No:</span> <%= receiptNo %></div> -->
<!-- <div class="row"><span class="label">Date:</span> <%= date %></div>

    <div class="section-title">Received From</div>
    <div class="row"><%= fullName %></div>

    <div class="section-title">Payment Details</div>
    <table>
      <tr>
        <td class="label">Subscription Plan:</td>
        <td><%= planName %></td>
      </tr>
      <tr>
        <td class="label">Subscription Type:</td>
        <td><%= subscriptionType %></td>
      </tr>
      <tr>
        <td class="label">Amount:</td>
        <td><%= amount %></td>
      </tr>
      <tr>
        <td class="label">GST (18%):</td>
        <td><%= gst %></td>
      </tr>
      <tr>
        <td class="label">Total Received:</td>
        <td><b><%= total %></b></td>
      </tr>
    </table>

    <div class="section-title">Amount in Words</div>
    <div><%= amountInWords %></div>

    <div class="section-title">Transaction Info</div>
    <div class="row">
      <span class="label">Payment Method:</span> <%= paymentMethod %>
    </div>
    <div class="row">
      <span class="label">Transaction ID:</span> <%= transactionId %>
    </div>

    <div class="section-title">Acknowledgement:</div>
<p>
  We hereby confirm receipt of the above payment towards <b>WrdsAI</b> access for your students.
</p>
<p>
  Access will remain active for the duration of the purchased term from the date of the receipt.
</p>

<div class="row" style="margin-top: 25px;">
  For any queries, please contact –
  <a href="mailto:support@wrdsai.com">support@wrdsai.com</a>
</div>

    
  </body>
</html> -->

<!DOCTYPE html>
<html>

<head>
  <meta charset="UTF-8" />
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 40px;
      line-height: 1.6;
    }

    /* Header Section */
    .header {
      width: 100%;
      display: flex;
      justify-content: flex-start;
      align-items: center;
      margin-bottom: 2px;
    }

    .header img {
      width: 190px;
      /* adjust size if needed */
      height: auto;
    }

    /* Line under header */
    .header-line {
      width: 100%;
      height: 1.5px;
      background-color: #000;
      /* black line */
      margin: 7px 0 10px 0;
    }

    .title {
      text-align: center;
      font-size: 26px;
      font-weight: bold;
      margin-bottom: 17px;
    }

    .section-title {
      font-size: 18px;
      font-weight: bold;
      margin-top: 25px;
      margin-bottom: 10px;
      border-bottom: 1px solid #555;
      padding-bottom: 5px;
    }

    .no-border {
      border-bottom: none !important;
    }

    .row {
      margin-bottom: 8px;
    }

    .label {
      font-weight: bold;
    }

    /* Payment Table */
    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 10px;
    }

    th,
    td {
      padding: 10px;
      border: 1px solid #777;
      text-align: left;
    }

    th {
      background: #f2f2f2;
      font-weight: bold;
    }
  </style>
</head>

<!-- src='${process.env.BACKEND_URL}/assets/words1.png' -->
<!-- src="${process.env.FRONTEND_URL}/words1.png" -->

<body>
  <div class="header">
    <img src="http://localhost:8080/assets/wrdsai.png" alt="WrdsAI Logo" width="140" />
  </div>

  <div class="header-line"></div>

  <div class="title">PAYMENT RECEIPT</div>

  <div class="row">
    <span class="label">Receipt No:</span>
    <%= receiptNo %>
  </div>

  <div class="row"><span class="label">Date:</span>
    <%= date %>
  </div>

  <div class="row"><span class="label">GST No.:</span> 27ABNCS5951R1Z8</div>

  <div class="row">
    <span class="label">Received From : </span>
    <%= fullName.toUpperCase() %>
  </div>

  <div class="section-title no-border">Payment Details</div>

  <table>
    <tr>
      <th>Field</th>
      <th>Details</th>
    </tr>
    <tr>
      <td class="label">Subscription Plan</td>
      <td>
        <%= planName %>
      </td>
    </tr>
    <tr>
      <td class="label">Subscription Type</td>
      <td>
        <%= subscriptionType %>
      </td>
    </tr>
    <tr>
      <td class="label">Amount</td>
      <td>
        <%= amount %>
      </td>
    </tr>
    <tr>
      <td class="label">Discount</td>
      <td>
        <%= discount %>
      </td>
    </tr>
    <tr>
      <td class="label">GST (18%)</td>
      <td>
        <%= gst %>
      </td>
    </tr>
    <tr>
      <td class="label"><b>Total Received</b></td>
      <td><b>
          <%= total %>
        </b></td>
    </tr>
  </table>

  <div class="row">
    <span class="label">Amount in Words : </span>
    <%= amountInWords %> INR
  </div>

  <div class="section-title">Transaction Info</div>
  <div class="row">
    <span class="label">Payment Method:</span>
    <%= paymentMethod %>
  </div>
  <div class="row">
    <span class="label">Transaction ID:</span>
    <%= transactionId %>
  </div>

  <div class="section-title">Acknowledgement</div>
  <p>
    We hereby confirm receipt of the above payment towards
    <b>WrdsAI</b> access. Access will remain active for the duration of the
    purchased term from the date of the receipt.
  </p>

  <div class="row" style="margin-top: 25px">
    <strong>Note:</strong> Payment once made is not refundable.
  </div>

  <div class="row" style="margin-top: 25px">
    For any queries, please contact –
    <a href="mailto:support@wrdsai.com">support@wrdsai.com</a>
  </div>
</body>

</html>